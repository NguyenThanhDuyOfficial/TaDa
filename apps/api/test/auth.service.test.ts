import { AuthService } from "../src/modules/auth"

describe("AuthService", () => {
  describe("register", () => {
    it("should create new user and session, return access token json, save refresh token to cookie", async () => {

    })
  })
})
