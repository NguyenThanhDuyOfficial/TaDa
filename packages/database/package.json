{
  "name": "database",
  "packageManager": "yarn@4.14.1",
  "scripts": {
    "dev": "yarn dlx prisma dev",
    "migrate": "dotenv -e ../../.env yarn dlx prisma migrate dev",
    "generate": "dotenv -e ../../.env yarn dlx prisma generate",
    "pull": "dotenv -e ../../.env yarn dlx prisma db pull --schema=packages/database/prisma/schema.prisma",
    "push": "dotenv -e ../../.env yarn dlx prisma db push --schema=packages/database/prisma/schema.prisma"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "prisma": "^7.8.0"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "dotenv-cli": "^11.0.0",
    "pg": "^8.20.0"
  }
}
